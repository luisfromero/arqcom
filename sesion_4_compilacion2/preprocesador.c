#include <math.h>
#define PEPE for
#define min(a,b) ((a)<(b)?(a):(b))
#define max(a,b) ((a)>(b)?(a):(b))
#define NUM 23+4


float *a;
float *b;
float *v;
int main()
{
        PEPE(int i=0;i<min(NUM,1000);i++)
        {
                v[i]=a[i]+b[i];
        }
}
